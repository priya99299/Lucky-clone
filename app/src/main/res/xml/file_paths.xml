<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- This matches getExternalFilesDir(Environment.DIRECTORY_DOWNLOADS) -->
    <external-files-path name="downloads" path="Download/" />

    <!-- This matches getExternalFilesDir(Environment.DIRECTORY_DOCUMENTS) -->
    <external-files-path name="documents" path="Documents/" />

    <!-- This matches getExternalFilesDir(Environment.DIRECTORY_PICTURES) -->
    <external-files-path name="pictures" path="Pictures/" />

    <!-- Extra: allow all external-files if needed -->
    <external-files-path name="external_files" path="." />
</paths>
